buttonGroup             Creates a button group component
dropNulls               Given a vector or list, drop all the NULL items
                        in it
hello                   Hello, World!
resetButtonGroup        Resets a button group to a state where all
                        buttons are selected and non are in implied
                        state
updateButtonGroup       Send an update message to a URL input on the
                        client. This update message can change the
                        value and/or label.
